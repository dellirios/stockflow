# StockFlow Environment Variables
# Copie este arquivo para .env e configure as variáveis

# GitHub Token para versionamento e auto-update
# Obtenha em: https://github.com/settings/tokens
# Permissões necessárias: repo (para repositórios privados) ou public_repo (para públicos)
GITHUB_TOKEN=your_github_token_here

# Configurações do servidor
PORT=5000
NODE_ENV=production

# Configurações de impressora (opcional)
DEFAULT_PRINTER_IP=192.168.1.100
DEFAULT_PRINTER_PORT=9100

# Configurações de log (opcional)
LOG_LEVEL=info
LOG_FILE=logs/stockflow.log